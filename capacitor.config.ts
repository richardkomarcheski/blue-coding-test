import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'blue-coding-test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
